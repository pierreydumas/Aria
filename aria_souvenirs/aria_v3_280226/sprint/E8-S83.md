# E8-S83 — SECURITY.md Lean: 5 Hard Rules Header Only

**Sprint:** E8  
**Type:** Documentation Refactor  
**Priority:** P2 — loaded by aria (main) and devops; 415 lines, heaviest file  
**Effort:** XS  

## Problem

SECURITY.md is the **heaviest file at 415 lines**. Loaded by `aria` (all 8 files) and `devops`. Issues:

- The 5 hard rules are the only behavioral content Aria needs at runtime
- 300+ lines of Python code examples are developer reference, not runtime behavioral guidance
- The ASCII architecture diagram is 25 lines of tokens with no runtime value
- Config / env vars / best practices / testing = pure reference
- All code examples exist in `aria_mind/security.py` itself — no need to duplicate them here

## Work To Do

### Lean header (~25 lines)

```markdown
# SECURITY — Hard Rules

1. **Prompt injection** → block at HIGH/CRITICAL; log via `AriaSecurityGateway`
2. **Credential leak** → never output API keys/passwords/tokens; always `OutputFilter` before responding
3. **SQL injection** → parameterized queries only; never concatenate user input into SQL
4. **Path traversal** → reject `../` in all file path inputs
5. **Rate limit** → 429 after RPM/RPH exceeded; enforce per-user via `RateLimiter`

| Threat | Module | Action |
|--------|--------|--------|
| Prompt injection (15 patterns) | `PromptGuard` | BLOCK + log |
| Credential in output | `OutputFilter` | REDACT |
| SQL injection | `InputSanitizer` | REJECT |
| Path traversal | `InputSanitizer` | REJECT |
| Rate abuse | `RateLimiter` | 429 |
| HIGH / CRITICAL | `AriaSecurityGateway` | BLOCK + alert |

**Skill**: `aria-input-guard` · **Module**: `aria_mind/security.py` · **Tests**: `tests/test_security.py`
```

### Reference block (~390 lines collapsed)

Wrap everything after the hard rules table:
- Full architecture diagram
- All component code examples (PromptGuard, InputSanitizer, RateLimiter, OutputFilter, SafeQueryBuilder, AriaSecurityGateway)
- Integration points (cognition, boundaries, API middleware, skill)
- Environment variable config
- Threat levels table
- Audit logging example
- Testing instructions
- Best practices
- Files created/modified table

## Acceptance Criteria

- [ ] SECURITY.md always-loaded section ≤ 30 lines
- [ ] 5 hard rules numbered list at top (immediate scan)
- [ ] Threat → Module → Action table visible immediately
- [ ] All 390 lines of code examples preserved in Reference block
- [ ] devops agent can still find all implementation details in Reference

## Token Impact

415 → ~30 lines always-loaded = **93% reduction** — biggest single saving in the sprint.
